import Docker from "dockerode";
import { Service } from "./index";
export declare class EthRpcService extends Service {
    port: number;
    constructor(commandArgs?: string[], blockProcess?: boolean);
    from_binary(pathToBinary: string): Promise<void>;
    from_docker(docker: Docker, nodePort: number): Promise<void>;
    stop(): Promise<void>;
    static waitForEthRpcToBeReady(port: number, maxAttempts?: number): Promise<void>;
}
