import Docker from "dockerode";
import { Service } from "./index";
export declare class SubstrateNodeService extends Service {
    port: number;
    constructor(commandArgs?: string[], blockProcess?: boolean);
    from_binary(pathToBinary: string): Promise<void>;
    from_docker(docker: Docker): Promise<void>;
    stop(): Promise<void>;
    static waitForNodeToBeReady(port: number, maxAttempts?: number): Promise<void>;
}
